# Supabase Edge Function settings (configure as project secrets; never expose in HTML)
ALLOWED_ORIGINS=https://origenix.netlify.app
INVITE_REDIRECT_URL=https://origenix.netlify.app/origenix-sistema-login.html

# Optional future notification integration. No address is invented by this project.
ADMIN_APPROVAL_EMAIL=
